{
  "name": "arabasta",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "format": "prettier --cache --write .",
    "format-check": "prettier --cache --check ."
  },
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
